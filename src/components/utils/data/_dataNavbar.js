const navbarStyle = [
    {
        navbar: 'one-menu',
    },
    {
        navbar: 'two-menus',
    },
]
const selectedNavbar = 'one-menu'

const getNavbarStyle = () => navbarStyle
const getSelectedNavbar = () => selectedNavbar

export { getNavbarStyle, getSelectedNavbar }
